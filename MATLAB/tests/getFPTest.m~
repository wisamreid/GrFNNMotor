classdef getFPTest < matlab.unittest.Testcase
    % ExampleTest 
    %   Template for Matlab Unittests
    
    properties
        OriginalPath
        marginOfError = 0.01;
    end
    
    methods (TestMethodSetup)
        function addLibToPath(testCase)
            testCase.OriginalPath = path;
            addpath(fullfile(pwd,'../lib'));
        end
    end
    
    methods (Test)
        function testFigure3A1(testCase)
            [actRegime,actRstar, actPsiStar] = getFP(1, 1, 0, -100, 0, 0, 0.2);
            expRegime = 1;
            expRstar = 
            expPsiStar = 
            testCase.verifyEqual(actRegime,expRegime,'AbsTol',sqrt(eps));
            testCase.verifyEqual(actRstar,expRstar,'AbsTol',sqrt(eps));
            testCase.verifyEqual(actPsiStar,expPsiStar,'AbsTol',sqrt(eps));

        end
    end
    
end

